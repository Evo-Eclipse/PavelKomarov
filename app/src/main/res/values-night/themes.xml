<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PavelKomarov" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>

        <item name="android:colorBackground">@color/neutral_background</item>
        <item name="android:statusBarColor">@color/neutral_surface</item>
        <item name="android:navigationBarColor">@color/neutral_background</item>

        <item name="android:textColorPrimary">@color/neutral_text</item>
        <item name="android:textColorSecondary">@color/neutral_surface</item>

        <item name="android:colorError">@color/red</item>
    </style>
</resources>